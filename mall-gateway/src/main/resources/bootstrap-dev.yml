spring:
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_ADDR:http://localhost:8848}
      config:
        server-addr: ${NACOS_ADDR:http://localhost:8848}
        file-extension: yaml
